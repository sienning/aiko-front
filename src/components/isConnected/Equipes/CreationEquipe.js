import React, { Component } from 'react';
import { Container, Grid } from 'semantic-ui-react'

const CreationEquipe = ({ userId }) => {
    return (
        <div style={{ marginTop: 60 }}>
            Création d'une équipe
        </div>
    );
}

export default CreationEquipe;
